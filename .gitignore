# macOS writes these into every folder it opens in Finder
.DS_Store
**/.DS_Store

# Claude Code local tooling. machine-specific, and launch.json points at
# temp paths that do not survive a reboot. CLAUDE.md is the exception: it
# carries this repo's rules into every Claude session, on any machine.
.claude/*
!.claude/CLAUDE.md
