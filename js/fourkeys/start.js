// ---- FOUR KEYS: where it begins -------------------------------------------------
// Last of the scripts, after the engine has built itself a stage and started
// its loop. brandon.html opens on its first wall; this opens on the town, and
// every level is walked into from there.
//
// Everything above this file only describes the game. This is the one line
// that starts it, which is why it gets a file of its own.
'use strict';

menuOpen();
